FILE(GLOB CSources *.c)
ADD_EXECUTABLE(${PROG_NAME} ${CSources})

ADD_DEFINITIONS("-DCONFIG_PATH=\"${CONFIG_PATH}\"")

INSTALL (	TARGETS ${PROG_NAME}
			DESTINATION bin
			PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE
						GROUP_EXECUTE
						WORLD_EXECUTE
						SETUID
		)
